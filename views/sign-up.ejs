<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('header.ejs') %>
    <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>opaq</title>
  <link rel="icon" type="image/png" href="assets/opacity-01.png">

  <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
  <style>
        html, body {
  width: 100%;
  height: 100%;
  /* Ensure the HTML and body elements take up the full height and width of the viewport */
}

body {
  background: linear-gradient(-45deg, #ffffff, #ffcfcf, #ffffff, #ffff6f);
  /* Define a linear gradient background with four colors */
  background-size: 400% 400%;
  /* Set the background size to 400% by 400%, creating a larger area for the gradient to animate within */
  animation: gradient 10s ease infinite;
  /* Apply the 'gradient' animation to the body element, specifying a duration of 15 seconds, easing function, and infinite iteration */
}

@keyframes gradient {
  0% {
    background-position: 0% 50%;
    /* At the start of the animation, position the gradient at the left-center */
  }
  50% {
    background-position: 100% 50%;
    /* At the midpoint of the animation, position the gradient at the right-center */
  }
  100% {
    background-position: 0% 50%;
    /* At the end of the animation, return the gradient to the left-center */
  }
}

      </style>
</head>
<section class="bg-white">
  <div class="lg:grid lg:min-h-screen lg:grid-cols-12">
    <aside class="relative block h-16 lg:order-last lg:col-span-5 lg:h-full xl:col-span-6">
      <img
        alt=""
        src="https://opaq.pages.dev/assets/pattern-01.png"
        class="absolute inset-0 h-full w-full object-cover"
      />
    </aside>

    <main
      class="flex items-center justify-center px-8 py-8 sm:px-12 lg:col-span-7 lg:px-16 lg:py-12 xl:col-span-6"
    >
      <div class="max-w-xl lg:max-w-3xl">
        <a class="block text-blue-600" href="#">
          
        </a>

        <h1 class="mt-6 text-2xl font-bold text-gray-900 sm:text-3xl md:text-4xl">
          Welcome to ⬡paq!
        </h1>

        <p class="mt-4 leading-relaxed text-gray-500">
          Lorem, ipsum dolor sit amet consectetur adipisicing elit. Eligendi nam dolorum aliquam,
          quibusdam aperiam voluptatum.
        </p>

        <form action="/sign-up" class="mt-8 grid grid-cols-6 gap-8 " method="post">
          <div class="col-span-6 sm:col-span-3">
            <label for="floatingInput" class="block text-sm font-medium text-gray-700">
              First Name
            </label>

            <input
              type="text"
              id="floatingInput"
              name="firstName"
              class="mt-1 w-full rounded-md border-gray-200 bg-white text-sm text-gray-700 shadow-sm"
            />
          </div>

          <div class="col-span-6 sm:col-span-3">
            <label for="floatingInput" class="block text-sm font-medium text-gray-700">
              Last Name
            </label>

            <input
              type="text"
              id="floatingInput"
              name="lastName"
              class="mt-1 w-full rounded-md border-gray-200 bg-white text-sm text-gray-700 shadow-sm"
            />
          </div>

          <div class="col-span-6">
            <label for="floatingInput" class="block text-sm font-medium text-gray-700"> phone </label>

            <input
            required pattern="[0-9]+"
              type="number"
              id="floatingInput"
              name="phone"
              class="mt-1 w-full rounded-md border-gray-200 bg-white text-sm text-gray-700 shadow-sm"
            />
            
          </div>

          <div class="col-span-6">
            <label for="floatingInput" class="block text-sm font-medium text-gray-700"> Email </label>

            <input
            
              type="email"
              id="floatingInput"
              name="email"
              class="mt-1 w-full rounded-md border-gray-200 bg-white text-sm text-gray-700 shadow-sm"
            />
          </div>

          <div class="col-span-6 sm:col-span-3">
            <label for="floatingInput" class="block text-sm font-medium text-gray-700"> Password </label>

            <input
              type="password"
              id="floatingInput"
              name="password1"
              class="mt-1 w-full rounded-md border-gray-200 bg-white text-sm text-gray-700 shadow-sm"
            />
            <small id="passwordHelp" class="form-text text-muted">Password must be at least 8 characters long and contain at least one uppercase letter, one lowercase letter, and one digit.</small>

          </div>

          <div class="col-span-6 sm:col-span-3">
            <label for="floatingInput" class="block text-sm font-medium text-gray-700">
              Password Confirmation
            </label>

            <input
              type="password"
              id="floatingInput"
              name="password2"
              class="mt-1 w-full rounded-md border-gray-200 bg-white text-sm text-gray-700 shadow-sm"
            />
          </div>

          <div class="col-span-6">
            <label for="MarketingAccept" class="flex gap-4">
              <input
                type="checkbox"
                id="MarketingAccept"
                name="t&c"
                class="size-5 rounded-md border-gray-200 bg-white shadow-sm"
                required
              />

              <span class="text-sm text-gray-700">
                <p class="text-sm text-gray-500">
                  By creating an account, you agree to our
                  <a href="#" class="text-gray-700 underline"> terms and conditions </a>
                  and
                  <a href="#" class="text-gray-700 underline">privacy policy</a>.
                </p>              </span>
            </label>
          </div>

          <div class="col-span-6 sm:flex sm:items-center sm:gap-4">
            <button
              class="inline-block shrink-0 rounded-md border border-blue-600 bg-blue-600 px-12 py-3 text-sm font-medium text-white transition hover:bg-black hover:text-blue-600 focus:outline-none focus:ring active:text-blue-500"
            >
              Create an account
            </button>

            <p class="mt-4 text-sm text-gray-500 sm:mt-0">
              Already have an account?
              <a href="/login" class="text-gray-700 underline">Log in</a>.
            </p>
          </div>
          <% if (error) { %>
            <div class="alert alert-danger">
                <p><%= error %></p>
            </div>
        <% } %>
        </form>
      </div>
    </main>
  </div>
</section>
</body>
</html>
  
